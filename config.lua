Config = {}

Config.WaitTime = 7000 -- How long to wait at the location

Config.PreloadLocations = {
    {
        pos = vec3(-979.68, -2633.12, 89.44), -- Location of camera
        lookAt = vec3(0.0, 0.0, 0.0) -- Where the camera will look at
    },
}